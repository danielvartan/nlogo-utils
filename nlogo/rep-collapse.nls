; __includes ["check-atomic.nls" "check-integer.nls" "collapse.nls"]

to-report rep-collapse [#x #times]
  assert-atomic #x
  assert-integer #times

  report collapse (n-values #times (list #x)) "" false
end
