; __includes ["check-string.nls"]
; extensions [pathdir string]

to-report basename [#path]
  assert-string #path

  let split string:split-on pathdir:get-separator #path

  if (empty? last split) [
    while [empty? last split] [
      set split butlast split
    ]
  ]

  report last split
end
