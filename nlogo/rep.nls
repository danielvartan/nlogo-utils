; __includes ["check-atomic.nls" "check-integer.nls"]

; IMPORTANT: Avoid using this procedure, use `n-values` instead.

to-report rep [#x #times]
  assert-atomic #x
  assert-integer #times

  report n-values #times [#x]
end
